import { OrderColumnPipe } from './order-column.pipe';

describe('OrderColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
