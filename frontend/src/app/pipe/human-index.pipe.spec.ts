import { HumanIndexPipe } from "./human-index.pipe";

describe('HumanIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
