import { RemoveCharPipe } from "./remove-char.pipe";

describe('RemoveCharPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveCharPipe();
    expect(pipe).toBeTruthy();
  });
});
